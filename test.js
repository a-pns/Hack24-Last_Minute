var articleSectionIds = [];
var changeArticleSections = {};
for(var i=0; i < articleSections.length; i++) {
  articleSectionIds[i] = articleSections[i].attr('id');
}
for(var i=0; i < articleSectionIds.length; i++) {
  console.log(i, articleSectionIds[i]);
}

for(var i=0; i < entities.length; i++) {
  while(indexOf(entities[i].name) > -1) {

  }
}

if(document.innerHTML.indexOf(testEntities[0].name) > -1) {
  index = document.innerHTML.indexOf(entities[0].name);
  var thisEl = this;
  thisEl.addClass('entity1');
}
